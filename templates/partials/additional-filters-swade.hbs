<div class="type-filter filter-row">
    <label for="type-filter">{{{localize "ACTOR_BROWSER.FilterType"}}}</label>
    <select id="type-filter" name="type-filter" type="dropdown" placeholder="Filter Type...">
        {{selectOptions additionalFiltersData.actorTypes selected=additionalFiltersData.typeFilter localize=true valueAttr="id" labelAttr="label"}}
    </select>
</div>
<div class="edge-filter filter-row">
    <label for="edge-filter">{{{localize "ACTOR_BROWSER.FilterEdge"}}}</label>
    <select id="edge-filter" name="edge-filter" type="dropdown" placeholder="Filter Edge...">
        {{selectOptions additionalFiltersData.edges selected=additionalFiltersData.edgeFilter localize=true valueAttr="id" labelAttr="label"}}
    </select>
</div>
<div class="ability-filter filter-row">
    <label for="ability-filter">{{{localize "ACTOR_BROWSER.FilterAbility"}}}</label>
    <select id="ability-filter" name="ability-filter" type="dropdown" placeholder="Filter Ability...">
        {{selectOptions additionalFiltersData.abilities selected=additionalFiltersData.abilityFilter localize=true valueAttr="id" labelAttr="label"}}
    </select>
</div>